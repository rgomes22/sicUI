import { GridHelperDirective } from './grid-helper.directive';

describe('GridHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new GridHelperDirective();
    expect(directive).toBeTruthy();
  });
});
